Supabase RLS / helpers — apply order
====================================

ONE PASTE (recommended for production / SQL Editor)
-------------------------------------------------
Run this single file only (idempotent; do not also run the incremental *_roster_access_rls.sql
files afterward — they repeat the same policies):

  supabase/paste_trip_roster_rls_bundle.sql

It defines private.current_profile_role + private.user_is_assigned_or_rostered_for_trip,
then policies for: profiles, trips, trip_team_members, trip_travel_safety (+ acknowledgments),
trip_activity, user_documents + storage.objects (worker-documents bucket), travel_form_responses,
trip_announcements.

Requires: those tables (and storage bucket) already exist.


Split files (same result as the paste bundle, for repo maintenance)
---------------------------------------------------------------------
1. private_trip_access_helpers.sql
2. profiles_rls.sql
3. trips_rls.sql
4. trip_team_members_rls.sql
5. trip_travel_safety_rls.sql
6. trip_activity_rls.sql
7. user_documents_rls.sql
8. travel_form_responses_rls.sql
9. trip_announcements_rls.sql

trip_team_members_worker_tshirt_update_rls.sql — redundant if you run full trip_team_members_rls.sql.

Incremental roster patches (policies only; require helpers first) — redundant after paste_trip_roster_rls_bundle.sql:
  trip_travel_safety_roster_access_rls.sql
  trip_activity_roster_access_rls.sql
  user_documents_worker_rls.sql
  travel_form_responses_roster_access_rls.sql

Legacy: trip_announcements_leader_rls.sql — superseded by trip_announcements_rls.sql in the bundle.


Manual smoke tests
------------------
- Roster-only worker: SELECT trip + roster + announcements; own roster row update; travel safety ack,
  activity, travel form, user docs as designed.
- Leader (assignment or roster): announcements write per policy.
- Staff: Trips team edit loads full roster; save does not wipe rows.
- JWT email differs from profiles.email: role + roster email match still work.
